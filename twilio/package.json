{
  "name": "assistant-twilio",
  "private": true,
  "description": "Twilio Functions that route calls to the Assistant iPhone app",
  "scripts": {
    "deploy": "twilio-run deploy --service-name assistant --override-existing-project",
    "setup": "node setup.mjs"
  },
  "dependencies": {
    "twilio": "^5.3.0"
  },
  "devDependencies": {
    "twilio-run": "^4.1.0"
  },
  "engines": {
    "node": "18"
  }
}
